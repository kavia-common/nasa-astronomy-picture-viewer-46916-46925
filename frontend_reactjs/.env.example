# PUBLIC_INTERFACE
# Environment variables required by the RetroSpace APOD frontend
# Copy to .env and adjust values as needed.

# Backend base URL (no trailing slash). Example for local FastAPI backend:
REACT_APP_BACKEND_URL=http://localhost:3001/api
